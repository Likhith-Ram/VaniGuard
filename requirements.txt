# VaniGuard — pinned dependencies
# numpy < 2.0 required: librosa 0.10.x is not yet compatible with numpy 2.x
streamlit>=1.35.0
librosa>=0.10.0
numpy>=1.24.0,<2.0
pandas>=2.0.0
matplotlib>=3.7.0
soundfile>=0.12.1
onnxruntime>=1.17.0
scipy>=1.11.0
miniaudio>=1.57           # in-memory MP3/WAV/FLAC/OGG decoder; no ffmpeg needed

# FastAPI backend (for mobile app API)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # required for file uploads in FastAPI
websockets>=12.0
